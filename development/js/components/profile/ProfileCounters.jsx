import React from 'react'
import PropTypes from 'prop-types'

class ProfileCounters extends React.Component {

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                {'There will be all counters of user statistic'}
            </div>
        )
    }
}

ProfileCounters.propTypes = {
    
};

export default ProfileCounters